import Popup from "./Popup.js";

export default class PopupWithImage extends Popup {
  constructor(PopupSelector, { card: name, card: src }) {
    super(PopupSelector);
    //cardSelector this._cardPopup =
  }
  open() {}
}
